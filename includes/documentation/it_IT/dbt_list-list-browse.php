<?php
/**
 * header-type:doc
 * header-title: La visualizzazione della lista
* header-tags: Browse list
* header-description: Visualizzare il risultato di una query salvata.
* header-lang:ITA
*/
namespace DatabaseTables;
if (!defined('WPINC')) die;
?>
<div class="dbt-content-margin">
    <p>I dati estratti da una lista sono gestiti da una query che può essere modificata dal tab setting. Tuttavia modificare la query di una lista può risultare un'operazione pericolosa perché infuisce su tutte le configurazioni, quindi finché è possibile sarebbe meglio cercare di cambiare la query di una lista il meno possibile.</p>
    <p>La configurazione delle colonne di una lista può essere invece modificata dal tab "List view formatting".</p>
    <p>La visualizzazione del plugin database tables è pensato per essere modificato solo dagli amministratori del sito, se si vuole permettere la gestione del contenuto di una lista ad altri utenti bisogna andare sul tab "setting" e attivare l'opzione "Show in admin menu".<br>Questa crea un link nel menu amministrativo per la lista specificata</p>
</div>
